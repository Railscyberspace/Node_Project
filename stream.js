let name = "Ruby's Mummy";
sayHi = () =>
{
    console.log("Hello" + ' ' + name);
}

sayHi(name);


let counter = (arr) =>
{
    // return 'There are ' + arr.length + ' ' + 'Element in this arrary';

};

let add = () =>
{
    return `The Best Age i will like to see Ruby see Start Learning Programing is ${ 2 + 2 }`;
};

let float = 43.5788;

module.exports.counter = counter;
module.exports.add = add;
module.exports.float = float;
